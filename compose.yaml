networks:
  app-network:
    driver: bridge
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: react-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/home/react/app
      - /home/react/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
    networks:
    - app-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    container_name: genie-backend
    ports:
      - "8000:8000"
      - "80:80"
    volumes:
      - ./backend:/home/genie/app
    environment:
      - GENIE_ENV=dev
      - JULIA_REVISE=on
    networks:
    - app-network

  api-gateway:
    image: tiangolo/uvicorn-gunicorn-fastapi:python3.11
    container_name: api-gateway
    depends_on:
      - backend
      - mesh-utils
      - frontend
    ports:
      - "8080:80"
    volumes:
      - ./gateway:/app
    environment:
      - WORKERS_PER_CORE=1
      - MAX_WORKERS=2
      - LOG_LEVEL=info
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost/health || exit 1"]
      interval: 120s
      timeout: 10s
      retries: 3
    networks:
    - app-network

  mesh-utils:
    build:
      context: ./MeshingService
      dockerfile: Dockerfile
    container_name: mesh-utils
    ports:
      - "8001:8001"
    volumes:
      - ./MeshingService:/home/genie/app
    environment:
      - GENIE_ENV=dev
      - JULIA_REVISE=on
    networks:
    - app-network