env: ENV["GENIE_ENV"]
# TODO: Make the veriables be pulled from environment file. For some reason, the
#       docker file doesn't seem to send the env variables to here.
dev:
  adapter: PostgreSQL
  database: postgres
  host: database
  username: postgres
  password: postgres
  port: 5432
  config:

prod:
  adapter:
  database:
  host:
  username:
  password:
  port:
  config:

test:
  adapter:
  database:
  host:
  username:
  password:
  port:
  config: